# POS Report Query

Write a Laravel Eloquent query or report for: $ARGUMENTS

Requirements:
- Use Eloquent with eager loading (no N+1)
- Accept date range parameters (from/to)
- Group and aggregate as needed (sum, count, avg)
- Return a structured Collection or array suitable for display
- Suggest how to display in a Blade view or export to Excel/PDF
